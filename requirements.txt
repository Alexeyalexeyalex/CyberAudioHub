# --- CyberAudio Hub: зависимости Python ---
# Установка:  pip install -r requirements.txt
#
# Обязательных зависимостей всего две, всё остальное — стандартная библиотека:
# база данных работает на встроенном модуле sqlite3.
#
# Проект протестирован на Python 3.12, Flask 3.1 и Werkzeug 3.1.

Flask>=3.0,<4.0
# Импортируется напрямую (werkzeug.security — хеширование паролей),
# поэтому указан явно, хотя и ставится вместе с Flask.
Werkzeug>=3.0,<4.0

# --- Необязательное: распознавание речи для текстовой версии книг ---
#
# Без одного из этих пакетов текстовый режим работает в демо-варианте:
# он показывает, как устроена подсветка, но НЕ распознаёт речь.
# Достаточно установить любой ОДИН движок.
#
# Рекомендуемый вариант (быстрее и экономнее по памяти).
# Модель скачивается автоматически при первом запуске распознавания:
# faster-whisper>=1.0
#
# Эталонная реализация Whisper, тяжелее и требует ffmpeg:
# openai-whisper
#
# Лёгкая альтернатива для слабого железа. Модель нужно скачать вручную
# с https://alphacephei.com/vosk/models и указать путь в CYBERAUDIO_VOSK_MODEL:
# vosk

# Для vosk и openai-whisper дополнительно нужен ffmpeg в системе:
#   Ubuntu/Debian:  sudo apt install ffmpeg
#   macOS:          brew install ffmpeg
#   Windows:        https://ffmpeg.org/download.html
